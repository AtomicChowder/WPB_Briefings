#!/usr/bin/env bash
# briefing-render — render docs/{user}/index.html for both users.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

python src/render.py adam
python src/render.py surali

echo "[render] done"
